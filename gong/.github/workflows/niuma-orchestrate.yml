name: niuma - Orchestrate

on:
  issues:
    types: [labeled]
  pull_request:
    types: [closed]
  repository_dispatch:
    types: [niuma.task.completed]
  schedule:
    - cron: "*/5 * * * *"

permissions:
  issues: write
  contents: read

jobs:
  orchestrate:
    if: >
      (github.event_name == 'issues' && (github.event.label.name == 'bot:queued' || github.event.label.name == 'bot:pr-reviewable')) ||
      (github.event_name == 'repository_dispatch' && github.event.action == 'niuma.task.completed' && github.event.client_payload.event_source == 'close-after-integration-merge') ||
      (github.event_name == 'pull_request' && github.event.action == 'closed') ||
      github.event_name == 'schedule'
    uses: biantaishabi2/Cli/.github/workflows/niuma-orchestrate-reusable.yml@f52f2d781d5a0762e9c838fb5231fa25c3790e57
    secrets: inherit
    with:
      repo: biantaishabi2/gong
      workflow_source_repo: biantaishabi2/Cli
      workflow_source_ref: master
      repo_dir: "."
      build_niuma: false
      label_whitelist: "bot:queued,bot:pr-reviewable"
      enable_dispatch_wakeup: true
      event_id: ${{ github.event.client_payload.event_id || '' }}
      dedup_window_hours: 24
      concurrency_key: "niuma-orchestrate-${repo}"
      enable_local_cleanup: false
