# Cli 项目哲学

> **LLM 时代的架构操作系统：让代码自我描述、自我验证、自我演进**

---

## 一句话总结

Cli 是 LLM 时代的**软件工程操作系统**：taskctl 编排复杂工作流，BCC 将代码结构编译为可验证的知识图谱，niuma 实现从需求到合并的全自动开发，BDDC 执行行为驱动测试。人定义规则和目标，机器处理执行细节和持续验证，软件工程从手工业走向基础设施化。

---

## 详细阐述

### 一、哲学意义：从"人理解代码"到"代码理解自身"

### 传统范式

```
┌─────────┐
│    人    │
└────┬────┘
     │ 阅读代码
     ▼
┌─────────┐
│  理解架构  │
└────┬────┘
     │ 记忆约束
     ▼
┌─────────┐
│  手工检查  │ ←────┐
└────┬────┘      │
     │           │ 循环依赖人的认知
     └───────────┘
     
瓶颈：人的认知有限，易错、易遗忘、不可扩展
```

### LLM 新范式（Cli 实现）

```
┌─────────┐
│   代码   │
└────┬────┘
     │ extract
     │ 自我结构化
     ▼
┌─────────┐
│  知识图谱  │
│ (SQLite) │
└────┬────┘
     │ arch validate
     │ 自动验证
     ▼
┌─────────┐
│  持续守护  │ ←────┐
└────┬────┘      │
     │           │ 代码成为自身的
     └───────────┘ 权威描述
     
转变：约束可执行、可验证，不依赖人脑记忆
```

**核心转变**：架构约束从"人脑中的规则"变成"可执行的基础设施"

---

## 二、时代意义：LLM 时代的"架构免疫系统"

### 历史演进

| 时代 | 核心挑战 | 基础设施 |
|------|---------|---------|
| 汇编时代 | 机器码可读性 | 编译器 |
| 互联网时代 | 快速开发 | 框架/库 |
| 云原生时代 | 规模化部署 | 容器/K8s |
| **LLM 时代** | **代码规模爆炸 + 架构腐化** | **Cli（架构验证基础设施）** |

### 为什么是"免疫系统"？

```
传统开发：
写代码 → 人 review（看不过来）→ 架构慢慢腐化 → 技术债务

LLM + Cli：
写代码/AI 生成 → BCC 自动验证（每次提交）→ 违规即拦截 → 架构持续健康
         ↑
    niuma 全自动：Issue → Code → Validate → Merge
```

---

## 三、对 LLM 范式的回应

| LLM 时代挑战 | Cli 的回应 |
|-------------|-----------|
| AI 生成代码质量参差 | BCC `arch validate`：自动门禁，不合规不通过 |
| 代码规模指数增长 | `graph-index`：持久化图谱，支持大规模查询 |
| 架构知识流失 | 图谱即文档，约束即代码，不依赖人脑记忆 |
| 多语言混合项目 | BCC 多语言提取，统一架构视图 |
| 快速迭代中的回归 | `bugfix`：改动历史 → 自动回归测试 |

---

## 四、Brownfield 的深层含义：演进式架构守护

不是"提取旧代码抄一遍"，而是：

```
每次迭代：
当前代码结构（新旧混合）
    ↓ extract
统一知识图谱（无差别对待）
    ↓ arch validate
架构约束检查（一视同仁）
    ↓
合规 → 继续演进
违规 → 拦截修复
```

**新写的代码和旧代码，进同一个验证体系，受同一个架构约束。**

---

## 五、终极意义：软件工程的去中心化

### 传统中心
- 架构师 = 瓶颈
- 文档 = 易腐烂
- 人 review = 不可扩展

### Cli 实现的去中心
- 架构约束 = 可执行代码（BCC）
- 知识 = 持久化图谱（SQLite）
- 验证 = 自动化（CI 门禁）
- 迭代 = 机器人（niuma）

**人的角色**：定义规则 → 批准方向  
**机器的角色**：执行验证 → 持续守护

---

## 六、一句话总结

> **Cli 是 LLM 时代的"架构操作系统"：让代码自我描述、自我验证、自我演进，人只负责定义规则和最终决策。**

这是软件工程从"手工业"走向"基础设施化"的关键一步。

---

## 工具链分层

```
┌─────────────────────────────────────┐
│  Layer 3: 智能体层 (niuma)           │
│  Issue → Plan → Code → PR → Merge   │
│  人只做：建 issue + 最终批准          │
├─────────────────────────────────────┤
│  Layer 2: 验证层 (BCC)               │
│  架构编译器：代码 → 图谱 → 验证 → 报告 │
│  AI 生成代码的"类型检查器"            │
├─────────────────────────────────────┤
│  Layer 1: 编排层 (taskctl)           │
│  任务依赖 DAG，多 issue 协调执行       │
└─────────────────────────────────────┘
```

---

*本文档描述 Cli 项目的长期愿景和哲学基础，具体实现细节请参考各子目录的 README。*
